[Unit]
Description=SmartAPI daily long-option trading bot
After=network-online.target trading-bot-bootstrap.service
Wants=network-online.target
Requires=trading-bot-bootstrap.service

[Service]
Type=simple
User=tradingbot
Group=tradingbot
WorkingDirectory=/opt/trading-bot
EnvironmentFile=/opt/trading-bot/.env
ExecStart=/opt/trading-bot/.venv/bin/python -m trading_bot.run_daily
Restart=on-failure
RestartSec=60
# systemd sends SIGTERM on stop (including when EventBridge stops the EC2
# instance) - run_daily.py handles that the same as Ctrl+C (SIGINT).
# 2026-09-24: every deploy ended with "Failed with result 'timeout'" and status=9/KILL - the
# runner's shutdown (closing the feed, persisting the capital ledger and open positions) takes
# longer than 30 s. A SIGKILL mid-position could skip that save.
TimeoutStopSec=120

[Install]
WantedBy=multi-user.target
